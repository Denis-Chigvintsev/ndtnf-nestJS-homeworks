import { WrapMethodInterceptor } from './wrap-method.interceptor';

describe('WrapMethodInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapMethodInterceptor()).toBeDefined();
  });
});
